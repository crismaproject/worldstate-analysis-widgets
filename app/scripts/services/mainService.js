// only for testing/demo
angular.module(
    'eu.crismaproject.worldstateAnalysis.services'
).factory(
    'eu.crismaproject.worldstateAnalysis.services.MainService',
    [
        function () {
            'use strict';
        }
    ]
);